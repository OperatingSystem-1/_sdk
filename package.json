{
  "name": "@mitosislabs/sdk",
  "version": "0.7.0",
  "description": "Mitosis — persistent AI agents that remember, replicate, and coordinate",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "mi": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "echo 'dist pre-built — skipping tsc'"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [
    "os1",
    "sdk",
    "offices",
    "agents",
    "integrations"
  ],
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "ethereum-cryptography": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
